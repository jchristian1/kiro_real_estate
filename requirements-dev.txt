# =============================================================================
# Dev and test dependencies — authoritative source of truth for local
# development, testing, and CI.
#
# Install:  pip install -r requirements-dev.txt
# Includes all runtime dependencies via -r requirements.txt.
# =============================================================================

-r requirements.txt

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-timeout>=2.1.0
hypothesis>=6.90.0
httpx>=0.25.0               # used in test clients

# --- Code quality ---
ruff>=0.1.0
mypy>=1.7.0

# --- Formatting (optional, used by pre-commit or CI) ---
black>=23.0.0
isort>=5.12.0
