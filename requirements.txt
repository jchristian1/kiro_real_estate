# =============================================================================
# Runtime dependencies — authoritative source of truth for Docker and bare
# local dev installs.
#
# Install:  pip install -r requirements.txt
# Docker:   Dockerfile installs this file directly.
#
# For dev/test tools see requirements-dev.txt.
# =============================================================================

# --- Database ---
SQLAlchemy>=2.0.0
Alembic>=1.12.0
psycopg2-binary>=2.9.0      # PostgreSQL driver; swap for psycopg2 in production builds from source

# --- Web framework ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6     # required by FastAPI for form/file upload parsing
starlette>=0.27.0           # FastAPI dependency; pinned to avoid silent upgrades

# --- Auth and security ---
bcrypt>=4.1.0
cryptography>=41.0.0        # Fernet encryption for Gmail credentials

# --- Data validation ---
pydantic>=2.0.0
pydantic[email]
email-validator>=2.0.0      # required by pydantic[email]

# --- Rate limiting and metrics ---
slowapi>=0.1.9
prometheus-client>=0.19.0

# --- Input sanitization ---
bleach>=6.0.0

# --- Configuration ---
python-dotenv>=1.0.0

# --- Async runtime ---
anyio>=3.7.0                # transitive dep of FastAPI/uvicorn; pinned for stability
